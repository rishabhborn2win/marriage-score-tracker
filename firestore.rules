rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows public read/write access for testing this application
    match /artifacts/{appId}/public/data/{document=**} {
      allow read, write: if true;
    }
  }
}